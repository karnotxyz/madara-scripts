{
  "name": "starknet-multi-transfer",
  "version": "1.0.0",
  "description": "Multi-account StarkNet transfer script",
  "main": "single-account-transfer.js",
  "scripts": {
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop all",
    "restart": "pm2 restart all",
    "delete": "pm2 delete all",
    "logs": "pm2 logs",
    "monitor": "pm2 monit",
    "clean": "rm -rf logs/* progress/* errors/*"
  },
  "dependencies": {
    "starknet": "7.0.0",
    "pm2": "^5.3.0"
  }
}